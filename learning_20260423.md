

# What's up today?

## api interceptor

It is used for settting up common request parameter like login credential.

1. define store.js in the `srcl, folder and defeine axios interceptor
2. re use it in the other component `import store from ../../store.js` 

