

<h1> What's Up today? </h1>


# Trigger
  이벤트를 저장하고자 할 때 쓰는 기능

create trigger <trigger name>
after [update|insert|delete] on <trigger target table name>
for each row
begin
    -- delete has no NEW
    [if old.<column name> <> new.<column name> then
        <sql insert>     
    end if; ...]  
end;


## customer_log
  - 고객이 정보를 수정할 때 기록되는 로그

## 고객회원 가입
  - 고객 비밀번호 암호화 라이브러리: bcrypt
  
## 고객 로그인

# 이미지 업로드 및 다운로드
  
  - multer 라이브러리 
  - 미들웨어사용시 
    ```js
    router.<http method>(
        'path for routing', 
        ~Middleware like multier~ , 
        (req, res) => { callback blah 

        }); 
    ```
    에서 req.<미들웨어가 추가한 속성> 이 존재. multer는 req.file을 생성