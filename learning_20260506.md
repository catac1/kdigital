

# What's up today?


# 백엔드 SQL 계속 공부

  - 물품 조회
  - 파라미터 req.query로 받아올때 Number변환 주의
  - SQL COUNT() 함수 안에서 alias 사용 불가
  - 백엔드에서 요청받을시 JSON과 Form을 구분
    - Postman JSON => body/raw json value
    - Postman Form => body/form add key value

# 프론트 엔드 Vite
  - npm run dev => localhost:5173
  - proxy setting => vite.config.js, rewrite is optional?
  - npm i axios react-router-dom antd @reduxjs/toolkit react-redux
  - cnt total 헛갈리지 말기

# NOTE
  - 백엔드에서 익셉션 처리 500을 해도 프론트에서 axios가 500을 만나면 익셥센이 뜬다.

# 머신러닝
   - scikit learn K-Nearest Neighbor(KNN)
  